# Обновление VM218431 — 25 сентября 2026

Выполнено по явному разрешению владельца. Новый SSH-ключ подтверждён владельцем через консоль VPS. Debian 13 x86_64. Дистрибутив: `/root/x-manager-update-20260925`.

| Требование | Реализация | Фактическая проверка |
|---|---|---|
| Сохранить настройки | Обновление существующих файлов без генерации новых реквизитов | Контрольные суммы конфигураций совпали; пользователи, токены, UUID, кодеки и Xray inbounds сохранены |
| Исключить 443/8443 для новых слушателей | Подбор разрешённого свободного порта | По отдельному разрешению WebDAV перенесён с 8443 на 18080; остальное сохранено |
| Исправить WebDAV multi | Права root:wdavtunnel, приватный YAML, доступный Python helper | Служба active, NRestarts=0, авторизованный PROPFIND 207 |
| Исправить Boards 415 | JSON Content-Type и Base64 content | openflux@2 active, NRestarts=0, подтверждён Boards WebSocket connected |
| Сохранить остальные каналы | Существующие URL/ключи/кодеки не переписываются | Каналы 1 Mail.ru и 3 CupsOnline active, NRestarts=0 |
| Сохранить подписки | Старый endpoint плюс отдельный JSON endpoint | /sub и /sub-json вернули HTTP 200 для существующего включённого пользователя |
| Добавить группы | Серверный модуль, атомарная таблица, TUI | Установленный TUI открывает существующего пользователя; локально 65 тестов подписок, включая 9 тестов групп |
| Откат при ошибке | Backup API SQLite, архив файлов, состояния служб, iptables | На VPS произошёл автоматический откат после ошибок окружения сборки и прав helper; основные службы восстановились |

Дополнительно исправлены обнаруженные именно при запуске через systemd условия: отсутствие HOME ломало Go module cache (теперь GOPATH/GOCACHE заданы явно); umask 077 закрывал общий каталог и скопированные помощники (теперь каталоги 0755, файлы 0644 задаются явно). В сообщении запуска OpenFlux скрыты URL и ключ.

Внешний запрос на `http://31.76.75.167:18080/` без авторизации вернул 401: новый порт доступен, анонимный доступ не разрешён. С прежними реквизитами локальная проверка вернула 207. Существующий TrustTunnel на 443 оставлен без изменений; новых слушателей на 443/8443 не добавлено, TCP/8443 свободен.

## Клиентское действие

Обновите порт локального VPS WebDAV в клиентских адресах с 8443 на 18080. Логин и пароль прежние. Сохранённые вручную URI пользователей не переписывались.

Группы: `x-sub` → пользователь → редактирование → пункт 12. Android-клиент не изменялся. Реальный импорт JSON и сквозной трафик с телефона через каждый протокол не проверены; состояние службы не заменяет такой тест.

## Откат

Последний автоматический снимок сохраняет уже согласованный порт 18080:

```bash
bash /root/x-manager-update-20260925/install.sh --rollback /var/backups/x-manager-B0SbsCf5
```

Снимок до любых изменений: `/var/backups/x-manager-before-20260925`. Он содержит прежний 8443 и прежние ошибки служб; не применяйте его как способ сохранить новую рабочую установку. Все резервные копии и журнал `/root/x-manager-update-20260925.log` доступны только root.

Изменения находятся в локальной ветке `codex/installer-recovery`, не опубликованы в GitHub. Повторная загрузка старого main с GitHub может вернуть прежний код.
